1.
function text() {
    return 'Labas';
}
console.log(text());

// 2.
function hi(say, n) {
    return say.repeat(n);
}
console.log(hi('labas ', 5));

// 3. 
function integer(num) {
    if (num % 2 === 0) {
        return
    }
}